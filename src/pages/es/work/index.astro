---
import { Image } from 'astro:assets'
import CardProject from '@components/cards/card-project.astro'
import { projects } from '@content/projects'
import Grille from '@icons/grille.astro'
import MainLayout from '@layouts/MainLayout.astro'
---

<MainLayout>
  <div class="px-4 pt-[14px] flex flex-col justify-center">
    <div class="relative flex flex-col mb-14">
      <div class="text-center max-w-[500px] m-auto p-6 flex flex-col items-center">
        <h4
          class="inline-block text-3xl mb-3 font-bold tracking-[-1.2px] bg-repeat bg-clip-text bg-100% bg-text-gradient"
          style="-webkit-text-fill-color:transparent"
        >
          Trabajo
        </h4>
        <p class="text-lg tracking-[-0.28px] leading-[140%]">
          Ha sido un absoluto placer poner mi corazón y alma en estos proyectos. Mientras esté aquí,
          explore estos proyectos.
        </p>
        <div class="flex justify-center items-center my-3">
          <Grille className="opacity-20 scale-y-[1] scale-x-[-1]" />
          <span class="w-max flex h-min">
            <Image
              loading="eager"
              class="invert mx-6"
              alt={'dots'}
              src="/three-dots.svg"
              width="35"
              height="35"
            />
          </span>
          <Grille className="opacity-20 " />
        </div>
      </div>
      <section class="pt-5">
        <div class="flex flex-col items-center justify-center">
          <div class="relative grid gap-[14px] mt-[6px] p-1 grid-cols-3 scale-[1.04]">
            {projects.map((project) => <CardProject project={project} isPageWork={true} />)}
          </div>
        </div>
      </section>
    </div>
  </div>
</MainLayout>
